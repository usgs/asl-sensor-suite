This is the default directory for RESP files.
RESP files can be taken from the IRIS NRL.
It is highly advised to store epochs in different RESP files,
because the program only parses in the last epoch in a file
